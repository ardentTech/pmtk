[package]
name = "pmtk"
version = "0.1.0"
edition = "2024"

[dependencies]
defmt = { version = "1.1.1", optional = true }
heapless = "0.9.3"
nom = { version = "8.0.0", default-features = false }


[features]
default = []
defmt = ["dep:defmt"]